services:
  web:
    build:
      context: app
      target: builder
    ports:
      - '80:5000'
    env_file:
      - .env
    volumes:
      - ~/.aws:/root/.aws:ro
    environment:
      - AWS_PROFILE=iesb
